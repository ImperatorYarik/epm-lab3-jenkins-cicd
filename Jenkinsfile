pipeline {
  agent any
  stages {
    stage('checkout') {
      steps {
        echo 'checkout'
      }
    }
    stage('build') { 
      steps { 
        echo 'build'
      }
    }
    stage('test') { 
      steps { 
        echo 'test'
      }
    }
    stage('build docker image') { 
      steps { 
        echo 'build docker image'
      }
    }
  }
}
